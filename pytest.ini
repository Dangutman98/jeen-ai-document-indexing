[pytest]
; Without this, `pytest` (the bare console script) fails to import `src.*`
; because -- unlike `python -m pytest` -- it does not add the current
; working directory to sys.path automatically. This makes both invocation
; styles behave identically regardless of how a user runs the suite.
pythonpath = .
testpaths = tests
